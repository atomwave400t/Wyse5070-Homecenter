# Default values for jellyfin.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  jellyfin:
    repository: "ghcr.io/jellyfin/jellyfin"
    # This sets the pull policy for images.
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"

  jackett:
    repository: "lscr.io/linuxserver/jackett"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"

  sonarr:
    repository: "lscr.io/linuxserver/sonarr"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"

  radarr:
    repository: "lscr.io/linuxserver/radarr"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"

  qbittorrent:
    repository: "lscr.io/linuxserver/qbittorrent"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"

  bookshelf:
    repository: "ghcr.io/pennydreadful/bookshelf"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "hardcover"

  rreadingglasses:
    repository: "blampe/rreading-glasses"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"

  rreadingglassesdb:
    repository: "postgres"
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "17"

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  jellyfin:
    port: 8096
    udpPort: 7359
    url: "http://jellyfin.krz.home"

  jackett:
    port: 9117
    name: "jackett"
    hostname: "jackett.krz.home"

  sonarr:
    port: 8989
    name: "sonarr"
    hostname: "sonarr.krz.home"

  radarr:
    port: 7878
    name: "radarr"
    hostname: "radarr.krz.home"

  qbittorrent:
    port: 6880
    udpPort: 6881
    name: "qbittorrent"
    hostname: "qbittorrent.krz.home"

  bookshelf:
    port: 8787
    name: "bookshelf"
    hostname: "bookshelf.krz.home"

  readingglasses:
    port: 8788
    name: "reading-glasses"
    hostname: "rreading-glasses.krz.home"

  readingglassesdb:
    port: 5432
    name: "rreading-glassesdb"

  type: ClusterIP

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: true
  host: "jellyfin.krz.home"
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/use-forwarded-headers: "true"

envVars:
  common:
    TZ: "Europe/Warsaw"
  qbittorrent:
    PUID: "1000"
    PGID: "1000"
  rreadingglasses:
    POSTGRES_HOST: "localhost"
    POSTGRES_DATABASE: "rreading-glasses"
    POSTGRES_USER: "rreading-glasses"
  rreadingglassesdb:
    POSTGRES_DB: "rreading-glasses"
    POSTGRES_USER: "rreading-glasses"

volumes:
  jellyfin:
    mountPath: 
      config: /config
      cache: /cache
      media: /media
  jackett:
    mountPath:
      config: /config
  sonarr:
    mountPath:
      config: /config
  radarr:
    mountPath: 
      config: /config
  qbittorrent:
    mountPath:
      config: /config
      downloads: /downloads
  rreadingglasses:
    mountPath:
      config: /
  rreadingglassesdb:
    mountPath:
      dbData: /var/lib/postgresql/data
